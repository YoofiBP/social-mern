import React from 'react';
import {AppBar, Toolbar} from "@mui/material";

const Home = () => {
    return (
        <AppBar position="static">
            <Toolbar>
                Home
            </Toolbar>
        </AppBar>
    )
}

export default Home;